shared:
    image: ubuntu:latest

jobs:
    main:
        requires:
            - ~pr
            - ~commit
            - ~sd@73:publish #artifact-bookend https://cd.screwdriver.cd/pipelines/74
        steps:
            - setup-package: apt-get update -y
            - install: apt-get install apache2 -y
            - start-server: apache2ctl -D foreground
